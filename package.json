{
  "scripts": {
    "clients:dev": "pnpm --recursive --parallel --filter=./clients/** dev",
    "clients:build": "pnpm --recursive --filter=./clients/** build",
    "clients:serve": "pnpm --recursive --parallel --filter=./clients/** serve",
    "services:dev": "pnpm --recursive --parallel --filter=./services/** start:dev",
    "services:build": "pnpm --recursive --filter=./services/** build",
    "services:serve": "pnpm --recursive --parallel --filter=./services/** serve"
  },
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^4.3.0",
    "@types/node": "^20.10.5",
    "prettier": "3.2.4",
    "serve": "^14.2.4",
    "typescript": "^5.2.2",
    "@nestjs/mapped-types": "*",
    "@trpc/client": "^11.8.0",
    "@trpc/server": "^11.8.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
